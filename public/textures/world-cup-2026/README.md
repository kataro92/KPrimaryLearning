# World Cup 2026 — textures

Optional image plates for the standalone game scene. Procedural canvas textures are used when these files are absent.

| File | Use |
|------|-----|
| `sky-stadium.png` / `.webp` | Far background sky |
| `turf-reference.png` / `.webp` | Pitch grass albedo |

Loaded at runtime from `src/standalone/world-cup-2026/graphics/optionalWorldTextures.ts`.
